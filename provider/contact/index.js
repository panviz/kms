/**
 * This provider can compile associated data into (human readable) "Contact" format
 */
var fs = require('fs')
, Path = require('path')
, _ = require('lodash')
, yaml = require('js-yaml')
, Graph = require('../graph/index')

var Contact = function () {
  var self = this
}

Contact.prototype.get = function () {
  var self = this
}

Contact.prototype.set = function () {
  var self = this
}

module.exports = Contact
